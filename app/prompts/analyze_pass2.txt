你在分析一个视频的分镜策略（第二遍：成文）。
第一遍已经给出了剧情梗概和关键时刻（见下）。现在附上选出的关键帧
单图（文件名即 f_NNN，NNN≈秒数+1），请你写出正式分析。

第一遍结果：
{{pass1}}

输出要求：
- shots：逐秒镜头清单，覆盖全片（不只关键帧），每行
  {"time": "00:0X", "shot": "画面/机位描述", "function": "这个镜头的叙事作用"}
- principles：从本片归纳出 5-10 条可复用的注意力调度原则，
  每条一句话标题 + 一两句说明，必须能落到具体镜头证据上
- 只输出一个 JSON 对象（不要输出其他任何文字）：
{
  "title_cn": "中文分析标题",
  "story": "剧情概括（可修正第一遍）",
  "shots": [{"time": "00:00", "shot": "...", "function": "..."}],
  "principles": [{"title": "...", "detail": "..."}],
  "keyframes_final": ["f_001", "f_012", "..."]
}
keyframes_final 从第一遍的 keyframes 里精简，保留论证必需的。
